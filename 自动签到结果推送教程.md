# 📣 自动签到结果推送教程

本教程介绍 **AstrBot 米游社每日签到插件** 的「每天自动签到完成后，把签到结果主动推送到你绑定的聊天会话」功能。

---

## 一、功能说明

插件每天按配置时间（默认 `09:00`）自动执行签到后，会生成一份签到结果，并**主动推送**到你绑定的聊天会话（QQ / Telegram / 微信 / 钉钉等），例如：

```
【米游社每日签到】
── 社区签到 ──
✅ 大别野 社区签到成功
── 游戏签到 ──
✅ 原神 旅行者(100000001) 签到成功，奖励 「摩拉」x20000
⏭️ 崩坏：星穹铁道 开拓者(100000002) 今日已签到，奖励 「星琼」x60
```

这样你**不用每天主动去问**，到点就能在聊天里看到签到情况。

---

## 二、如何绑定「推送会话」

插件会把结果推送到 **`bind_umo`** 指向的会话。设置方式如下（任选其一）：

### 方式 1：扫码登录自动绑定 ✅ 推荐

发送：

```
/mys扫码
```

用米游社 App 扫码登录成功后，插件会**自动把当前对话**记录为推送目标，无需额外操作。

### 方式 2：使用 `/mys绑定` 显式绑定

随时发送：

```
/mys绑定
```

插件会把**当前对话**设置为推送目标，并回复「✅ 已绑定当前会话」。适合你主动指定推送位置（例如想推送到某个群而不是私聊）。

### 方式 3：手动签到 / 测试时自动绑定

发送任意签到相关指令：

```
/mys签到
/mys测试
```

如果**之前从未绑定过**，插件会顺带把当前对话记为推送目标（若已绑定过则不会覆盖）。

### 方式 4：WebUI 手动填写（一般不需要）

在 AstrBot WebUI 的插件配置中有一个 `bind_umo` 字段（会话标识字符串，格式：`平台:消息类型:会话ID`，例如 `aiocqhttp:group_message:123456789`）。

> 一般**不需要手动改**，用方式 1 / 2 / 3 即可自动写入。

---

## 三、开关配置

在 AstrBot WebUI「插件管理 → 米游社每日签到」中：

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `push_result` | `false` | **是否推送结果**。设为 `true` 后，自动签到完成才会推送。`false` 则只写日志 |
| `auto_sign_enable` | `true` | 是否启用每天自动签到 |
| `auto_sign_time` | `09:00` | 自动签到时间（24 小时制，**服务器时区**） |

推送目标（`bind_umo`）也会显示在配置中，方便确认绑到了哪里。

> ⚠️ 关键：`push_result` 默认是**关闭**的。想要接收推送，请务必在 WebUI 里把它设为 `true`，并确保已绑定会话。

---

## 四、工作流程（技术原理）

```
时间到点 (auto_sign_time)
        │
        ▼
┌─ 定时循环 _auto_sign_loop()（插件 initialize() 时启动的 asyncio 任务）
        │
        ▼
┌─ do_sign_all()  依次执行：社区签到（大别野等）→ 游戏签到（原神/星穹铁道等）
        │
        ▼
┌─ _run_auto_sign()  检查 config["push_result"] == true
        │
        ▼
读取 config["bind_umo"]（绑定会话的唯一标识，格式：平台:消息类型:会话ID）
        │
        ▼
context.send_message(umo, MessageChain(...))  ← 主动发送到绑定会话
```

关键点：
- 会话标识 `bind_umo` 由 `event.unified_msg_origin` 自动生成，例如 `aiocqhttp:group_message:123456789`
- 主动发送使用 AstrBot 的 `context.send_message()` API
- 定时任务在插件 `initialize()` 生命周期中启动，插件卸载（`terminate()`）时自动取消
- 若 `push_result` 为 `false` 或未绑定会话，自动签到**仍然会执行**，只是不推送（结果写入 AstrBot 日志）

---

## 五、常见问题（FAQ）

### Q1：自动签到成功了，但没收到推送？
按顺序排查：
1. 检查插件配置 `push_result` 是否为 `true`（默认是 `false`！）
2. 确认已绑定会话：发送一次 `/mys绑定`，或重新 `/mys扫码`
3. 查看 AstrBot 日志，是否出现 **"未绑定会话，跳过推送"** 或 **"推送签到结果失败"** 的提示

### Q2：换了一个群 / 换了设备，推送去哪了？
推送目标是 `bind_umo` 记录的那个会话。想改推送位置：
- 在新会话里发送 `/mys绑定`（会**覆盖**更新推送目标）
- 或重新 `/mys扫码`（扫码成功后会绑定当前会话）

### Q3：推送失败，日志报某平台「不支持主动消息」？
部分平台（尤其是 **QQ 官方 API**）出于官方限制，**不允许机器人主动推送消息**。
解决办法：
- 改用 `aiocqhttp`（NapCat / Lagrange）等支持主动消息的平台
- 或在该平台用 `/mys签到` 手动签到查看结果

### Q4：推送的内容可以自定义吗？
当前推送的是完整签到结果文本（社区签到 + 游戏签到明细）。如需精简或定制格式，可修改插件 `main.py` 中 `do_sign_all()` 返回的内容。

### Q5：自动签到时间按什么时区？
按 **AstrBot 服务器的本地时区**（`datetime.now()`）。如果你的服务器是 UTC，需要把 `auto_sign_time` 相应换算（例如 UTC+0 想在北京时间 09:00 执行，应填 `01:00`）。

### Q6：机器人不在线时自动签到会执行吗？
只要 AstrBot 进程**持续运行**，定时任务就会执行。机器人掉线/重启后，若已错过今天的签到时间点，会顺延到**次日同一时间**执行（不会补签）。

---

## 六、配置速查

```yaml
# _conf_schema.json 中与自动签到推送相关的配置项
"push_result":       true      # 自动签到后推送结果到绑定会话（默认 false）
"auto_sign_enable":  true      # 每天自动签到
"auto_sign_time":    "09:00"   # 自动签到时间（服务器时区）
```

---

*更多用法见 [README.md](../README.md)。如遇问题，请查看 AstrBot 日志并附带错误信息反馈。*

---

## ⚠️ 免责声明

本插件为**第三方非官方插件**，仅供**学习交流和个人使用**。使用自动化签到工具可能违反米游社《用户协议》并触发官方**风控机制**，存在账号功能受限、要求验证甚至**封号**的风险。使用本插件所产生的任何账号风险、数据丢失或其他后果，均由使用者**自行承担**。请妥善保管你的登录凭证（Cookie/stoken），切勿公开分享。完整免责声明请查看 [DISCLAIMER.md](../DISCLAIMER.md)。
